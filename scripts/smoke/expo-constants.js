/** Minimal expo-constants stub for the Node harness (app version only). */
module.exports = {
  default: { expoConfig: { version: '1.1.0' }, appOwnership: 'expo' },
  expoConfig: { version: '1.1.0' },
};
